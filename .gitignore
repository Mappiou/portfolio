node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vercel
.vite

# APKs are distributed via GitHub Releases, not committed (keeps the repo light)
public/apks/*.apk

# Tests
coverage
playwright-report
test-results
.playwright
mobile-check/
